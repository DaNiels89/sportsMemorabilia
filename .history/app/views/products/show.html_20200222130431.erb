<h1>Show memorabilia</h1>

<% if @product.pic.attached? %>
	<%= image_tag @product.pic %>
<% end %>

<%= link_to "Edit", "/products/#{@product.id}/edit" %>

<%= button_to “Delete”,  @product, method: :delete %>